[project]
name = "attack"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=3.0.1",
    "nltk>=3.9.1",
    "playwright>=1.47.0",
    "vllm>=0.6.2",
]
